import React from 'react';
import PropTypes from 'prop-types';
import Nav from "./Nav";
const Header = props => {
  return (
    <div>
      <Nav />
    </div>
  );
};

Header.propTypes = {
  
};

export default Header;
